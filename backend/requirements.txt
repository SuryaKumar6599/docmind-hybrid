# ============================================================
# DocMind Backend — Python dependencies
# Install: pip install -r requirements.txt
# Tested on Python 3.11+ (Apple Silicon M4 + Linux x86_64)
# ============================================================

# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# --- Supabase (DB + Storage + Realtime) ---
supabase>=2.5.0

# --- LLM client (Ollama via OpenAI-compatible API) ---
openai>=1.40.0
requests>=2.32.0

# --- llama-cpp-python (optional — used when CHAT_PROVIDER=llamacpp) ---
# Requires hardware-specific compile flags for GPU acceleration:
#   Apple Silicon (Metal): CMAKE_ARGS="-DGGML_METAL=on" pip install "llama-cpp-python[server]>=0.3.0"
#   NVIDIA (CUDA):         CMAKE_ARGS="-DGGML_CUDA=on"  pip install "llama-cpp-python[server]>=0.3.0"
#   CPU-only (slow):       pip install "llama-cpp-python[server]>=0.3.0"
llama-cpp-python[server]>=0.3.0

# --- Structured LLM output — forces valid JSON via Pydantic (REQUIRED) ---
instructor>=1.3.0

# --- Pydantic v2 (schemas, settings, validation) ---
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- Document conversion (PDF, DOCX, PPTX, images → Markdown) ---
markitdown[all]>=0.0.1
pymupdf>=1.24.0          # scanned/image-only PDF fallback (renders pages → vision OCR)

# --- Token counting (enforce 8k context budget) ---
tiktoken>=0.7.0

# --- DOCX generation (Stage 3 — no LLM) ---
python-docx>=1.1.0

# --- Environment variables ---
python-dotenv>=1.0.0

# --- Testing ---
pytest>=8.0.0
httpx>=0.27.0

pydantic-yaml>=1.2.1
pydantic

# --- PDF Export ---
# NOTE: do NOT add 'docx-python' here — it shadows python-docx and breaks 'from docx import Document'

# ============================================================
# Optional — PDF export (Stage 3)
# pandoc must be installed via OS package manager, NOT pip:
#   macOS:  brew install pandoc
#   Ubuntu: sudo apt install pandoc
# ============================================================

# ============================================================
# Optional — Token compression (auto-used when JD/resume > budget)
# Uncomment to enable LLMLingua:
# llmlingua>=0.2.0
# ============================================================
beautifulsoup4>=4.12.0
